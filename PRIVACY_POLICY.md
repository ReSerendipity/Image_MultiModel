# Image MultiModel 隐私政策

**生效日期**：2026-09-02 ｜ **版本**：v1.0

Image MultiModel 是一款自托管的图像生成平台。我们遵循"本地优先、最小收集"原则，本政策说明数据处理方式。

## 1. 本地数据（默认不上传）

| 数据类型 | 处理方式 |
|---|---|
| 生成提示词（Prompt） | 仅发送至您配置的本地/自托管推理引擎（默认 native 引擎在本机运行），提示词不出设备 |
| 生成结果（图片） | 保存于本地 `outputs/` 目录，不上传任何服务器 |
| 历史记录（生成历史、任务队列） | 存储于本地数据库（SQLite），可随时清空 |
| 水印密钥、配置 | 存储于本机配置与密钥文件，不随仓库分发 |

## 2. 推理引擎的数据流向

- **默认（native / Z-Image-Turbo，进程内 ComfyUI 内核）**：推理在本机 GPU 完成，提示词与结果均不出设备；
- **可选远程引擎**：若您将服务配置为对外开放（局域网/公网部署），则按您的部署方式处理数据；访问者提交的提示词由您部署的服务处理，请自行评估相关合规要求；
- **可选 diffusers 备用引擎**：本机推理，同样不出设备。

## 3. 我们不收集的内容

本服务**不收集**任何个人信息、使用埋点或遥测数据；不依赖第三方分析服务。

## 4. 数据删除

删除本地 `outputs/` 目录与历史数据库即可清除全部生成数据。

## 4.1 水印密钥与来源标识（P0 补充声明）

- 水印密钥仅存于**本机**（`data/.watermark_key` 或环境变量注入），**不随仓库/安装包分发**；
- 正式分发版不预置任何密钥：每台设备首次启动时自生成**实例级水印密钥**，仅用于该设备生成内容的来源标识，其他设备无法读取；
- 来源标识（水印或 `.provenance.json` 侧车元数据）随生成图片保存在本地 `outputs/` 目录；嵌入失败时默认以侧车文件保留溯源信息，不阻断您的产出；
- 清单签名私钥仅存于签发机与构建流水线密钥库（离线备份），公钥随应用内置仅用于完整性校验，任何人无法凭公钥伪造签名。

## 5. 政策更新

本政策如有变更将在本页面同步更新并标注版本号。

---

*本政策为初稿参考，不构成正式法律意见。*
