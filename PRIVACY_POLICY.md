# Image MultiModel 隐私政策

**生效日期**：2026-09-02 ｜ **版本**：v1.0

Image MultiModel 是一款自托管的图像生成平台。我们遵循"本地优先、最小收集"原则，本政策说明数据处理方式。

## 1. 本地数据（默认不上传）

| 数据类型 | 处理方式 |
|---|---|
| 生成提示词（Prompt） | 仅发送至您配置的本地/自托管推理引擎（默认 diffusers 引擎在本机运行），提示词不出设备 |
| 生成结果（图片） | 保存于本地 `outputs/` 目录，不上传任何服务器 |
| 历史记录（生成历史、任务队列） | 存储于本地数据库（SQLite），可随时清空 |
| 水印密钥、配置 | 存储于本机配置与密钥文件，不随仓库分发 |

## 2. 推理引擎的数据流向

- **默认（diffusers / Z-Image-Turbo）**：推理在本机 GPU 完成，提示词与结果均不出设备；
- **可选远程引擎**：若您将服务配置为对外开放（局域网/公网部署），则按您的部署方式处理数据；访问者提交的提示词由您部署的服务处理，请自行评估相关合规要求；
- **旧版 native 引擎（已弃用）**：本地 ComfyUI 内核执行，同样不出设备。

## 3. 我们不收集的内容

本服务**不收集**任何个人信息、使用埋点或遥测数据；不依赖第三方分析服务。

## 4. 数据删除

删除本地 `outputs/` 目录与历史数据库即可清除全部生成数据。

## 5. 政策更新

本政策如有变更将在本页面同步更新并标注版本号。

---

*本政策为初稿参考，不构成正式法律意见。*
