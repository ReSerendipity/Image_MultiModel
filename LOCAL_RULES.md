# 本地仓库管理规则（本文件仅本地保留，已被 .gitignore 忽略，不会上远程）

1. 【铁律】本地内容一律不允许删除。任何删除操作必须先列出具体步骤给仓库所有者审核，获批准后才能执行。
2. 远程展示内容由 .gitignore 控制：不在本地删任何东西，只用 git rm --cached + .gitignore 规则控制远程内容。
3. 本仓库远程保留口径：核心代码与依赖、安装/启动脚本、README 及其引用图片、CI 测试相关内容、开源法律文件、demo/。
4. 本地独享内容（不上远程）：流程/分析文档、原型、示例、音色、封面图等，见 .gitignore 末尾规则块。

## 变更隔离（家族仓库适用，2026-08-27 家族治理补强）

- 本项目**只做本仓库**的改动；工作区内其他家族仓库（DraftPeek / MiniMax-H3-lite / SeedVR2-lite / SpiritPal / TTS_MultiModel）与本任务无关。
- **禁止**未经用户要求修改其他仓库的任何文件，含其 AGENTS.md / README.md / 规范文档。
- 跨仓库借鉴必须先在本仓 `docs/reports/` 留一份落地记录，再动对方文件。

## 远程同步铁律（Remote Sync Rule）

- 禁止未经说明直接修改远程 main；操作后必须检查并同步本地（git pull），禁止留下分叉。
- 用户本地有未提交修改时，不得擅自 commit / push / stash / 覆盖。
- 默认在本地修改、用户确认后 push。
