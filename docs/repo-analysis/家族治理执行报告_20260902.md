# 家族治理执行报告（2026-09-02）

> 性质：跨仓库（6 仓）综合报告——本轮"分阶段全量落地"的执行收口，含任务状态、AGENTS 一致性差异审阅（t8）、债务评估结论（t10），以及需用户裁决的开放项。
> 存放：`docs/repo-analysis/`（家族跨仓报告约定位置）。

---

## 1. 任务执行总览（t0 前置 + t1–t10）

| # | 任务 | 状态 | 说明 / 证据 |
|---|------|:---:|------------|
| 前置 | 补写 Image_MultiModel 桶级 AGENTS.md | ✅ | `Image_MultiModel/AGENTS.md`（v1.0 建立 → v1.1/v1.2 同步自纠） |
| t1 | 家族版本管理对照文档 | ✅ | `docs/repo-analysis/家族版本管理对照.md`；6 仓版本载体均实测 |
| t2 | Image config `workflow_file` vs workflows/ 一致性 | ✅ | 默认引擎字段置空；`tests/test_config.py` 同步；`workflows/` 为你确认删除的参考目录 |
| t3 | MiniMax comfy_kernel AGENTS 分工说明 | ✅ | MiniMax AGENTS §0 补特别声明；版本 v1.8→v1.9 |
| t4 | TTS lint `continue-on-error` + `_error_html` 标题 | ✅（已解决） | ci.yml 注释证实 2026-08-30 已收紧为阻断；`_error_html` 已支持 `title_key`（v1.16/v1.17） |
| t5 | SeedVR2 dependency-audit 哈希 + e2e `--update-snapshots` | ⚠️ 部分 | `--update-snapshots` 未复发（e2e.yml 有 NEVER 注释）；`dependency-audit` 跨平台哈希需真实 CI 确认 |
| t6 | SpiritPal `cmd_` 前缀 + 双端 generate_handler | ✅（无需改） | lib.rs 无 `cmd_` 前缀函数；双 `generate_handler!`（桌面+移动）为设计意图 |
| t7 | DraftPeek 双分支隔离 push 自查 | ✅（持续红线） | 其 AGENTS §8 已含完整自查清单；属"每次 push 前自检"纪律，无新增代码需求 |
| t8 | 家族 6 仓 AGENTS 一致性差异审阅 | ✅ | 见 §2 |
| t9 | 家族 6 仓文档与实际一致性核验 | ⚠️ 部分 | 本轮修 Image 两处 bin→app 残留；另发现 2 项需裁决（§3） |
| t10 | 长期债务（TTS mypy / SeedVR2 coverage M1）评估 | ✅（结论） | 见 §3 |

---

## 2. t8：家族 6 仓 AGENTS 一致性差异审阅

### 2.1 共同骨架（6 仓一致 ✅）
- §0 文档优先级 +「机器可验证事实优先」判据
- ⚠️ Agent 行为契约：**6 条自进化铁律**（同步/坑点/SOP/自检/版本递增/证据绑定）+ 启动自检清单
- 禁区目录表；测试约定；Git（Conventional Commits）+ CI；Known Gotchas + SOP；📋 自进化修订记录表（含"已校验"列）
- 文件归档与放置规范；远程同步铁律；push 前预检铁律；CI 流程铁律（推送闭环/修复交接/红灯止损）

### 2.2 差异点（审阅结论）与处理
| 维度 | 差异 | 本轮处理 |
|------|------|---------|
| 桶级 AGENTS 有无 | Image 此前缺失（只有 LOCAL_RULES.md） | ✅ 已补一桶级 AGENTS |
| comfy_kernel 上游 AGENTS 分工 | Image、MiniMax 都有 vendored `comfy_kernel/`，其 AGENTS 易被误当项目文件 | ✅ 两仓 §0 均已补特别声明 |
| Gotchas/SOP 是否拆分出 AGENTS | SeedVR2/TTS/SpiritPal 已拆到 `docs/project/`；MiniMax/DraftPeek/Image 内联 | 既有合理差异，**不建议强统** |
| 分支策略 | DraftPeek/SpiritPal 用 main 私有 + public 公开双分支（源玛严格隔离） | 各自合规，未改动 |
| 覆盖率门禁 | Image=65、SeedVR2=50、TTS=40、DraftPeek 未配 CI 阈值 | 见 §3 债务评估 |

> 结论：家族 6 仓 AGENTS 骨架已统一；差异属各仓技术栈/成熟度所致，本轮仅补齐 Image 缺失 + comfy_kernel 分工说明这两处真实缺口。

---

## 3. t10 + t9 开放项：债务评估与需裁决项

### 3.1 债务评估（结论）
| 项 | 评估 | 建议 |
|----|------|------|
| TTS mypy ~268 项 | 已用**棘轮基线**（`.ci/mypy_baseline.txt`）管理：只阻断"债务上升"，不阻断存量 | 维持棘轮；逐步下调基线固化成果；勿一次清零 |
| SeedVR2 覆盖率 M1=70 | 当前 fail_under=50，M1 目标 70；增量集中 GPU 依赖模块 | 需 GPU runner 补齐 native 测试后上调（路线图 doi 已有） |
| Image 覆盖率=65 | 数值高于家族；但存在**未绿门禁测试**（见 3.2） | 先消解 3.2 的 2 项再谈上调 |

### 3.2 本轮新发现、需你裁决的开放项
1. **Image 版本漂移测试**：`tests/test_config.py::test_app_config_builds` 断言 `version=="2.0.0"`，而 `config.yaml` 为 `1.2.2` → 测试与 config 不一致。**需裁决**：以 `1.2.2` 为准改测试，还是本应将版本提到 2.0.0？
2. **Image 缺失治理脚本**：`tests/test_config_refs_gate.py` 依赖 `scripts/check_config_refs.py`，该脚本**不存在** → 该门禁 3 例恒失败。**需裁决**：补写该脚本，还是删除/修正这个失效门禁测试？
3. **SeedVR2 dependency-audit（#37）**：Linux 上 torch 哈希缺口。无法在本机复验 CI，需你在有 CI 的环境确认是否仍红、再决定修法。

---

## 4. 本轮实际改动文件（回滚点）

- `Image_MultiModel/AGENTS.md`（新增；v1.0→v1.2）
- `Image_MultiModel/config.yaml`（`workflow_file` 置空；`integrity_manifest`/`locale_dir` 由 `bin/`→`app/`）
- `Image_MultiModel/tests/test_config.py`（`workflow_file == ""`）
- `Image_MultiModel/docs/repo-analysis/`（新增 版本台账 + 本报告）
- `MiniMax-H3-lite/AGENTS.md`（§0 补声明；v1.8→v1.9）

未改动：`comfy_kernel/**`、Image `release/**`（历史快照保留）、其余各仓业务代码/CI。