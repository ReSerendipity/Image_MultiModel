# 2026-09-01 — 安全评估整改收尾（Image_MultiModel v2.0.0）

## 上下文
用户连发「继续」，本以为要从零做安全整改，但核实发现本会话前几轮已把安全评估
（SECURITY_ASSESSMENT_v2.0.0.md，17 项）的 Critical/High/Medium 全部落地并提交：
C-01 认证(middleware/auth.py)、H-01 HTTPS(app_server.py:795 构造 uvicorn SSL)、
H-02 权重校验(native/engine.py:92)、H-03 内容过滤 NFKC+注入规则、H-04 清单补核心模块+
测试升级断言、H-05 MCP 接入 filter_image_generation、M-01/M-02/M-03/M-04/M-05/M-06/M-07、
以及根因门禁 scripts/check_config_refs.py（接入 ci.yml:304，校验 config.yaml security/runtime
段键必须被代码消费）。

## 本轮实际做的事
1. 核实每个「已实现」项是否真接入（防 phantom）：auth/security_headers 均注册进
   app_server.py，默认 enabled=false 保本地零配置；HTTPS/权重/内容过滤/MCP 均确认接线。
2. 修复唯一残留正确性缺口 **L-01**：integrity_selfcheck.py 在 skipped>0 时仍打「通过」，
   改为显式 WARNING（消除报告点名的「假通过」）。
3. 改完核心模块后**重算完整性清单**（scripts/generate_integrity_manifest.py，现 33 模块），
   自检因哈希失配报 failed 属预期，重算后即绿——端到端验证了完整性机制有效。
4. 跑验证：安全测试集 139 passed / 0 failed；CI 门禁 PASS。
5. 产出 `docs/SECURITY_REMEDIATION_TRACKER.md`（17 项状态表 + 根因门禁说明 + 日常纪律）。

## 结论
安全评估 17 项：16 项已修并验证（含根因门禁），L-01 本轮补完；L-02/L-03/L-04 为 Low，
本地优先形态残余风险低，标注可选加固（服务化时再补）。
工作树变更：integrity_selfcheck.py（L-01）+ integrity_manifest.json（重算），未提交，待人工 commit。

## 注意
- 改任何核心安全模块后必须重算清单，否则自检哈希失配报 failed（预期行为）。
- config.yaml security: 段加键必须被代码读，否则 check_config_refs.py CI fail。

# 2026-09-01 — 运维评估遗留项验证 + 冒烟门禁补强

## 上下文
用户再发「继续」。上一轮（08-31）运维评估留下两个待验证 P0：`idle_unload_minutes`
配置契约、`run_until_complete` 在运行循环内失效，并明确要求「发布前跑真实启动 smoke」。

## 结论
1. 两个 P0 **已被上游修复**：`config_models.py:289`/`config.yaml:241` 有字段且
   `check_config_refs.py` 设为强制回归点；卸载改为 `idle_unload_loop()` 协程 await。
2. 静态门禁：`check_config_refs` PASS；`check_spec_refs` **修复前退出码 3（1 幻影）
   → 0**。幻影是**误报**：README 写 `docs/THIRD\_PARTY\_NOTICES.md`（转义下划线），
   审计器把 `\` 归一成 `/`。已在 `.spec_audit/audit_spec_refs.py` 加「解析失败时按
   CommonMark 转义剥反斜杠重试」的第二释义，不影响 `docs\_devarchive` 类 Windows 路径。
3. 真实 smoke 6/6 PASS，但**发现绿灯假象**（Gotcha #47）：8288 上的实例以
   `IMM_FAKE_ENGINE=1` 启动，任务同样 completed（0.3s），产物是 `imm_fake_*` 临时目录
   里的 0 字节文件，`outputs/` 无任何新文件 → 只轮询 status 的 smoke 分不出真推理。
   已给 `scripts/post_deploy_smoke.py` 加 `--require-real-output`（校验非 imm_fake /
   非 0 字节 / 尺寸一致，失败项 `generation_output`，退出码 1），并在 `deploy.yml` +
   `bluegreen.sh:smoke_full()` 开启；`ci.yml` 无 GPU 保持关闭并加注释。
   反向验证已做：在 fake 实例上加该参数 → FAIL 且 exit 1。
4. 测试：tests/release + tests/smoke 66 passed；post_deploy_smoke 单测 16 → 20。

## 产出与变更
- 报告：`docs/reports/RELEASE_READINESS_VERIFICATION_20260901.md`
- 代码：scripts/post_deploy_smoke.py、tests/release/test_post_deploy_smoke.py、
  .github/workflows/{ci,deploy}.yml、scripts/deploy/bluegreen.sh
- 文档：docs/ops/staging_smoke.md、README.md、AI_DEV_SOPS.md（Gotcha #47/#48）
- 仓外工具：C:\Users\Doro\.spec_audit\audit_spec_refs.py（转义第二释义）

## 待办 / 风险
- 8288 实例是 FakeEngine，非真实推理服务；要出真图需另起不含 IMM_FAKE_ENGINE 的进程。
- 磁盘 CRITICAL：可用 8.7%（83.1GB/952.6GB），见 docs/runbooks/disk_full.md。
- 工作树仍有上一轮安全治理改动未提交（integrity_selfcheck.py / manifest /
  generate_integrity_manifest.py / tracing.py / SECURITY_REMEDIATION_TRACKER.md）。

# 2026-09-01 — 家族仓库安全治理流水线收尾（6 仓）

## 上下文
用户指令「按照你的建议，全量执行任务」= 提交 IM 安全整改 + 把
安全评估→根因门禁(check_config_refs.py)→整改 的流水线套用到其余 5 个家族仓库。

## 完成状态（截至本轮回合）
| 仓库 | 形态 | 门禁 | M-02 中间件 | phantom/回环修复 | 提交 |
|---|---|---|---|---|---|
| Image_MultiModel | Py/FastAPI | ✅ ci.yml:304 | ✅ | ✅ | 早前已提交 |
| TTS_MultiModel | Py/FastAPI | ✅ quality-gate | ✅ | — | 59bf05d（本回合补提交）|
| SeedVR2-lite | Py/FastAPI(dict 配置) | ✅ runtime.security | ✅ | — | ac3badb |
| MiniMax-H3-lite | Py/@dataclass Settings | ✅ dead-config gate | 潜伏(无 comfy spawn 点) | ✅ M1 HOST 回环 | 9eef4d4 |
| SpiritPal | Tauri/Rust+React | N/A | N/A | S1 sql:allow-execute / S2 默认口令 spiritpal123 | 仅审计文档 7a6995c |
| DraftPeek | Kotlin+Py | N/A | N/A | D1 硬编码 0.0.0.0 + 可选鉴权 | 仅审计文档 f0b8c26 |

## 本回合纠正（重要）
- 摘要误记 TTS 已提交；实际 git 为**已暂存未提交**(A/AM 5 文件)。本回合
  `git add` 终版门禁(black 格式化差异，无逻辑改动)后 commit 59bf05d，pre-commit 全过。
- 隔离并行会话未提交改动：TTS 的 docs/六大项目...md(untracked)、
  scripts/check_engine_compat.py(unstaged) 均未纳入本次提交。
- 各仓均用各自 .venv/Scripts/python.exe 跑门禁（managed python 缺 pyyaml/fastapi）。

## 已确认决策（用户拍板）
- SpiritPal(S1/S2) / DraftPeek(D1)：**保留只读审计文档，不落实际代码修复**（非 Python，门禁不适用，
  风险已在审计文档标注）。后续若要做专项修复再单独开。
- 6 仓本地提交：**不 push**，全部本地留存等人工 review；尤其私有仓 SpiritPal/DraftPeek 按计划不推。

## 结论
家族安全治理流水线收尾完成：4 个 Python/FastAPI 仓库(IM/TTS/SeedVR2/MiniMax) 门禁+整改
全部本地提交并验证；SpiritPal/DraftPeek 仅审计文档。无 push。

# 2026-09-01 — 真实推理验证 + 历史库落盘 root cause 修复 + 提交

## 完成（用户指令「全量执行」）
1. **真实引擎验证**：另起 8289（不带 IMM_FAKE_ENGINE）跑
   `post_deploy_smoke.py --checks generation --require-real-output --width 256 --height 256`。
   首次 PASS 但发现 **DB 把 file_size/width/height 落成 0**，磁盘上却是合法 9666 字节
   256×256 WEBP（outputs/z_image_turbo_native/20260901/000001a05d97f75c_0.webp）→ 校验误杀真部署。
2. **root cause 修复**：`app_server.py` 调 `history_db.add_output` 时未回填尺寸元数据。
   改为从磁盘 stat + PIL 读真实尺寸后落库；重启真实例复验，DB 现记录 9668 字节 / 256×256。
3. **冒烟校验健壮性**：`validate_outputs()` 在 DB 尺寸缺失（全 0）时回退磁盘 stat
   （含 PIL 读尺寸），真实部署不被误杀；通过说明标注「含磁盘回退 n 个」。
   反向验证：fake 实例（8288）加该参数 → FAIL + exit 1（仍能拦假引擎）。
4. **测试**：post_deploy_smoke 单测 20 → 22（新增「DB 尺寸缺失→磁盘回退通过」「文件缺失仍失败」）；
   tests/release + tests/smoke 68 passed；check_spec_refs 0 发现、check_config_refs PASS、ruff 全清。
5. **完整性清单**：app_server.py 改动后重算 integrity_manifest.json（33 模块），启动自检 33/33 绿。
6. **提交**：`19e88a2` on main，8 文件 / 225 增 / 8 删。

## 纠正（重要）
- 上节「工作树仍有上一轮安全治理改动未提交」已不成立：那些改动于早前提交
  （f78a77a 修 CI 行尾+清单、6524e77 整改追踪表）。本次仅 tracing.py 显 ` M`，
  但 `git diff` 空 → 是 `core.autocrlf` 纯行尾 phantom（工作树/HEAD 均 LF、内容一致），
  `git checkout --` 清除，**未提交**。
- `docs/六大项目文档整理与核对.md` 仍 untracked 且**刻意保持本地**（b812f69「远程移除、本地保留」），
  本回合未纳入提交。

## 风险（仍未决）
- 磁盘 CRITICAL：可用 8.7%（83.1GB/952.6GB），启动持续 firing DiskSpaceLow，见 docs/runbooks/disk_full.md。
- 当前 8289/8288 真实/假引擎实例**已停止**（netstat 确认无监听）。
