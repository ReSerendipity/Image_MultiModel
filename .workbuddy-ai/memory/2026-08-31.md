# 2026-08-31 — 安全合规评估 (Image_MultiModel v2.0.0)

## 任务
对 Image_MultiModel v2.0.0 做 OWASP Top 10 深度安全评估。已完成 15+ 文件阅读与
1 次 PathGuard 绕过 PoC (imm_poc_pathguard.py)，第二次 PoC (imm_poc2.py) 因包
含 PHP webshell 字节测试数据 `<?php system($_GET['c']); ?>`，被本地文件实时监
控（疑似 QQ 安全管家 / 火绒类）误判为 Backdoor/PHP.TinyShell.m (D70122762477B60B)。

## 关键发现（待写入最终报告）
1. **未实现但被宣称的功能**：config.yaml 声明 `basic_auth`、`api_token` 但无任
   何中间件实现 → 完全无身份认证；CSRF 只校验 token 存在，不与登录态绑定。
2. **权重校验只在两个地方接入**：`native/diffusers_engine.py` 与 `native/lora.py`；
   **默认引擎 `z_image_turbo_native`（native/engine.py）完全没调用 `verify_weight_before_load`**，
   即 `verify_weights: true` / `only_safetensors: true` 对默认引擎形同虚设。
3. **完整性自检 manifest 缺失关键模块**：`security/magic_check.py` 与
   `security/weight_integrity.py` 在 `_CORE_MODULES` 列表但不在 manifest → 启动时
   跳过；`security/content_filter.py` 根本不在核心模块列表。
4. **prompt 检查纯关键词列表 + `.lower()`**，可被空格插入/同形字/字符替换轻易绕过；
   **没有 prompt injection 防护**（IGNORE/DAN/<|im_start|> 全部放行）。
5. **PathGuard 主体稳健**：URL 编码、.. 穿越、空字节、UNC、绝对路径都拦截；
   软链会因 `p.resolve()` 跟随 → 解析后落出白名单 → 拦截（已实测）；唯一一处
   "伪正"是双重编码 `%252e` 因路径归一化失败变成字面 `%2e...`，安全但接口
   输出不规范。FTS5 MATCH 参数化，无注入。
6. **前端 XSS**：app.js 共 54 处 innerHTML/insertAdjacentHTML；`escHtml` 全转义
   函数存在且多数用于 prompt，但仍有：line 183 文件名只 escape `<`、line 420
   `e.display_name` 无转义、line 977 `r.detail` 无转义。
7. **CSRF**：httponly+double-submit 实现规范，前端正确带 X-CSRF-Token 头；但
   路由层缺白名单豁免——`/api/health`、`/api/events` (SSE) 属 GET 自动豁免；
   没有 Bearer / Cookie auth，因此 token 实质只是"前端在场"证明。
8. **mcp_server.py (22KB)**：尚未读取，需确认是否绕过上述中间件。
9. **Rate Limit**：内存 deque 无 key 过期回收（dict 累积，仅值过期），无
   `X-Forwarded-For` 信任配置（`request.client.host` 在反向代理后失效）。
10. **HTTPS**：`server.ssl.enabled=false` 默认关闭；推荐部署侧加 TLS 反代。

## 教训
PoC 测试数据包含 PHP webshell 字面量 → 触发杀软启发式告警。下次 PoC 应避免
写入真实攻击载荷字面字符串，改为 hex/base64/分片方式表达。

## 补充静态审计结论（PoC 中断后补完）
11. **认证零实现**：`basic_auth` / `api_token` 有配置有模型，全仓无任何中间件消费。
12. **HTTPS 零实现**：`server.ssl` 配置项无任何代码读取，两处 `uvicorn.run()` 都不传 SSL。
13. **MCP 通道绕过全链路**：`mcp_server.py` txt2img 0 处引用 content_filter/PathGuard/限流。
14. **`/api/tasks/export` 无 PathGuard**：直接 `Path(db_path)` 打包；当前因存相对路径
    + cwd 不匹配而导出空 ZIP（功能已坏），但 DB 污染即变任意文件读取。
15. **`sys.path.insert(0, comfy_kernel)`**：1454 个 vendored 文件无完整性校验；
    `custom_nodes_dir` 无白名单 → 配置篡改可劫持任意模块导入。
16. **`max_size_mb: 2000` 零消费** → 上传无大小限制，解压炸弹 DoS。
17. **`clean_launch.py` 硬编码外部项目解释器**（SeedVR2-lite / TTS_MultiModel）并
    `os.execv` 重启；自动 `pip install` 无 hash 锁定。
18. **测试空断言**：`test_security_audit.py:143` 只验证 manifest 文件存在，不断言
    key 集合 == _CORE_MODULES；`test_content_filter.py` 无绕过用例；无 prompt injection
    测试；无真软链测试；`tests/frontend/` 只有 smoke.js。
19. **已确认安全**：SQL 全参数化（含 FTS5 MATCH 绑定）；无硬编码凭据；error_handler
    不泄露堆栈；无 0.0.0.0 监听（CI 有门禁）；magic_check 正确排除 SVG。

## 交付
报告已生成：`SECURITY_ASSESSMENT_v2.0.0.md`（17 项问题：1 Critical / 5 High /
7 Medium / 4 Low，按 OWASP Top 10 2021 分类 + 三阶段修复路线图）。

## 状态
- PoC #1 结果可信已用；PoC #2 触发杀软告警未跑完，改静态分析。
- **已交付报告**。核心结论：真正风险不是单点漏洞，而是"配置-实现不一致"——
  未兑现的控制项不报错、不告警、测试也不失败。最高优先级修复是建立
  CI 门禁扫描 config.yaml 中每个安全项是否被代码引用。

# 2026-08-31 — 运维稳定性体系评估

## 任务
对 Image_MultiModel 用户指定的 v2.0.0 运维稳定性体系做深度完整性评估，覆盖监控告警、SLA/SLO、故障响应、容量弹性、部署回滚和五类反模式。

## 结论
已生成 `OPS_STABILITY_ASSESSMENT_v2.0.0.md`。按运维控制能力评分：MTTD 16/30、MTTR 15/30、可用性 19/40，总分 50/100。项目具备单机推理服务的健康检查、队列反压、任务恢复、GPU 采样和局部引擎回滚基础，但缺少统一 metrics/告警、正式 SLO、Runbook/on-call/post-mortem、自动扩缩容、蓝绿/金丝雀和应用级回滚。

## 关键新增发现
当前工作树的成本治理改动中，`app_server.py` 直接访问未在 `RuntimeConfig` 与 `config.yaml` 定义的 `idle_unload_minutes`，可能形成启动阻断；自动卸载路径在运行事件循环内调用 `run_until_complete`，可能永远不生效。发布前应优先修复并运行真实启动 smoke。