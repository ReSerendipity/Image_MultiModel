# Docker build context 排除清单

# 1) comfy_kernel/ 是 ComfyUI（GPL-3.0，上游 https://github.com/Comfy-Org/ComfyUI）
#    的本地独立副本（独立 git 仓库，未被本仓库跟踪）。
#    为避免镜像捆绑 GPL 代码，构建镜像时不复制该目录；
#    运行时由 docker-compose.yml 只读挂载 ./comfy_kernel:/app/comfy_kernel 提供。
#    若确有内嵌需求：须随镜像附带 GPL-3.0 全文并注明镜像整体受 GPL-3.0 约束，
#    且须遵守 GPL-3.0 的全部义务（含提供对应源代码）。
comfy_kernel/

# 2) 本地仓库元数据与缓存（不影响构建）
.git/
__pycache__/
*.pyc
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
# 3) 运行期由 compose 卷挂载提供的大目录（云原生评估反模式 #5）：
#    权重/产物不入镜像层 —— 避免体积暴涨与权重泄露进镜像
model/
pretrained_models/
data/
outputs/
logs/
workflows/

# 4) 本地环境与归档（非运行必需）
.venv/
WPy64-*/
backups/
screenshots/
demo/
